% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_single_proteome_da_comparison.R
\name{run_single_proteome_da_comparison}
\alias{run_single_proteome_da_comparison}
\title{Run Full Protein-Level Differential Analysis for a Single Comparison}
\usage{
run_single_proteome_da_comparison(
  comparison,
  limma_params,
  normalized_counts,
  out_dirs,
  org_db = NULL,
  intensity_matrix_raw = NULL,
  ont_option = "BP",
  skip_gsea = FALSE,
  protein_metadata = NULL,
  heatmap_norm = "zscore",
  color1 = "#D55E00",
  color2 = "#0072B2"
)
}
\arguments{
\item{comparison}{Named list with elements \code{name}, \code{exp}, and \code{ctrl}.}

\item{limma_params}{Named list with elements \code{E} (intensity matrix) and
\code{design} (model matrix).}

\item{normalized_counts}{Numeric matrix or data frame (proteins x samples) of
imputed log2 intensities passed to \code{\link{generate_heatmap}}.}

\item{out_dirs}{Named list of output directories as returned by
\code{\link{setup_directories}}.}

\item{intensity_matrix_raw}{Numeric matrix or data frame of pre-imputation intensities
(with \code{NA}s) used to compute imputation categories. Pass \code{NULL} to skip.}

\item{ont_option}{Character. GO ontology for GSEA: \code{"BP"} (default), \code{"MF"},
or \code{"CC"}.}

\item{skip_gsea}{Logical. If \code{TRUE}, GSEA is skipped entirely. Default
\code{FALSE}.}

\item{protein_metadata}{Data frame with a \code{uniprotswissprot} column for joining
additional annotation (e.g. \code{PG.Genes}). Pass \code{NULL} to skip.}

\item{heatmap_norm}{Character. \code{"zscore"} (default) or \code{"intensity"} for
the heatmap colour scale.}

\item{color1}{Character. Primary plot colour (up-regulated / imputed).
Default \code{"#D55E00"}.}

\item{color2}{Character. Secondary plot colour (down-regulated / observed).
Default \code{"#0072B2"}.}
}
\value{
Named list with elements:
\describe{
\item{limma}{Data frame of annotated differential analysis results.}
\item{gsea}{\code{gseaResult} object or \code{NULL} if GSEA was skipped or
failed.}
\item{highlighted_ids}{Character vector of protein IDs labelled on the volcano
plot.}
}
Returns \code{NULL} if the analysis fails entirely.
}
\description{
Run Full Protein-Level Differential Analysis for a Single Comparison
}
\details{
Orchestrates the end-to-end protein analysis pipeline for one contrast:
\enumerate{
\item Runs limma differential analysis via \code{\link{perform_limma_analysis}}.
\item Optionally joins protein metadata (e.g. gene names).
\item Optionally annotates results with imputation categories based on pre-imputation
NA counts.
\item Saves differential results to \code{out_dirs$de_data}.
\item Generates volcano, MA, and heatmap figures.
\item Optionally runs GSEA via \code{\link{process_gsea}} and saves results.
}
}
