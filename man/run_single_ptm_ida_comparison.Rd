% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_single_ptm_ida_comparison.R
\name{run_single_ptm_ida_comparison}
\alias{run_single_ptm_ida_comparison}
\title{Run Full PTM Integration Analysis for a Single Comparison}
\usage{
run_single_ptm_ida_comparison(
  comparison,
  limma_params,
  ptm_imp_matrix,
  protein_imp_matrix,
  out_dirs,
  intensity_matrix_raw = NULL,
  peptide_metadata = NULL,
  ont_option = "BP",
  ensembl = NULL,
  org_db = NULL,
  skip_gsea = FALSE,
  heatmap_norm = "zscore",
  color1 = "#D55E00",
  color2 = "#0072B2"
)
}
\arguments{
\item{comparison}{Named list with elements \code{name}, \code{exp}, and \code{ctrl}.}

\item{limma_params}{Named list with elements \code{E} (intensity matrix) and
\code{design} (model matrix), used to identify sample group membership.}

\item{ptm_imp_matrix}{Numeric matrix or data frame (PTM peptides x samples) of
imputed log2 intensities, with columns \code{peptide_id} and \code{PG.UniProtIds}.}

\item{protein_imp_matrix}{Numeric matrix or data frame (proteins x samples) of
imputed log2 intensities, with column \code{PG.ProteinAccessions}.}

\item{out_dirs}{Named list of output directories as returned by
\code{\link{setup_directories}}.}

\item{intensity_matrix_raw}{Numeric matrix or data frame of pre-imputation intensities
(with \code{NA}s) used to compute imputation categories. Pass \code{NULL} to skip.}

\item{peptide_metadata}{Data frame with at minimum \code{peptide_id} and
\code{PG.UniProtIds} columns for joining annotation. Pass \code{NULL} to skip.}

\item{ont_option}{Character. GO ontology for GSEA: \code{"BP"} (default), \code{"MF"},
or \code{"CC"}.}

\item{ensembl}{A \code{Mart} object from \code{biomaRt::useMart} used for gene symbol
annotation. Pass \code{NULL} to skip BioMart lookup.}

\item{skip_gsea}{Logical. If \code{TRUE}, GSEA is skipped entirely. Default
\code{FALSE}.}

\item{heatmap_norm}{Character. \code{"zscore"} (default) or \code{"intensity"} for
the heatmap colour scale.}

\item{color1}{Character. Primary plot colour (e.g. down-regulated / imputed).
Default \code{"#D55E00"}.}

\item{color2}{Character. Secondary plot colour (e.g. up-regulated / observed).
Default \code{"#0072B2"}.}
}
\value{
Named list with elements:
\describe{
\item{limma}{Data frame of differential analysis results.}
\item{gsea}{\code{gseaResult} object or \code{NULL} if GSEA was skipped or failed.}
\item{protein_counts}{Data frame of per-protein PTM counts used for GSEA, or
\code{NULL}.}
\item{highlighted_ids}{Character vector of peptide IDs highlighted on the volcano
plot.}
}
Returns \code{NULL} if the analysis fails entirely.
}
\description{
Run Full PTM Integration Analysis for a Single Comparison
}
\details{
Orchestrates the end-to-end PTM analysis pipeline for one contrast:
\enumerate{
\item Filters PTM and protein matrices to shared, unambiguous UniProt IDs.
\item Pivots both matrices to long format.
\item Calls \code{\link{perform_multitest_ptm_ida_analysis}} for protein-corrected
differential analysis.
\item Optionally joins peptide metadata and BioMart gene symbols.
\item Optionally annotates results with imputation categories.
\item Saves differential results to \code{out_dirs$de_data}.
\item Generates volcano, MA, and heatmap figures.
\item Optionally runs GSEA via \code{\link{process_gsea}} and saves results.
}
}
