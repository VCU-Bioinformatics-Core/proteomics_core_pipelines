% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{generate_global_heatmap}
\alias{generate_global_heatmap}
\title{Generate a Global Heatmap of All Detected Molecules}
\usage{
generate_global_heatmap(
  intensity_matrix,
  out_dirs,
  top_n = 1000,
  molecule_label = "Proteins",
  heatmap_norm = "zscore",
  color1 = "#D55E00",
  color2 = "#0072B2"
)
}
\arguments{
\item{intensity_matrix}{Numeric matrix or data frame (features x samples) of log2
intensities. Rows with non-finite values are dropped before clustering.}

\item{out_dirs}{Named list of output directories as returned by
\code{\link{setup_directories}}.}

\item{top_n}{Integer. Maximum number of rows (by CV) to include. Default \code{1000}.}

\item{molecule_label}{Character. Label used in plot titles and axis (e.g. \code{"Proteins"},
\code{"Peptides"}). Default \code{"Proteins"}.}

\item{heatmap_norm}{Character. Normalisation method: \code{"zscore"} (default) scales each
row to mean 0 / sd 1; any other value uses raw intensities.}

\item{color1}{Character. Reserved colour for consistency with other plot helpers.
Default \code{"#D55E00"}.}

\item{color2}{Character. Reserved colour for consistency with other plot helpers.
Default \code{"#0072B2"}.}
}
\value{
Character string giving the full path to the saved PNG file.
}
\description{
Generate a Global Heatmap of All Detected Molecules
}
\details{
Filters to the top \code{top_n} most variable rows by coefficient of variation,
applies z-score or raw intensity normalisation, clusters rows and columns, and writes
a PNG to \code{out_dirs$heatmap/global_heatmap.png}.
}
