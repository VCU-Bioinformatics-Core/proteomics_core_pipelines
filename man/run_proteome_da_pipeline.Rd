% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_proteome_da_pipeline.R
\name{run_proteome_da_pipeline}
\alias{run_proteome_da_pipeline}
\title{Run the regular (protein-level) differential abundance pipeline}
\usage{
run_proteome_da_pipeline(
  run_id,
  protein_matrix_file,
  samplesheet_file,
  out_dir,
  genome = "mouse",
  imputation_method = "DEP-MinProb",
  imputation_q = 0.01,
  imputation_seed = 42,
  heatmap_top_n = 1000,
  heatmap_norm = "zscore",
  gsea_ont = "BP",
  skip_gsea = FALSE,
  skip_anova = FALSE,
  group_color1 = "#D55E00",
  group_color2 = "#0072B2"
)
}
\arguments{
\item{run_id}{Character. Unique identifier for this analysis run;
used in output file names and the HTML report title.}

\item{protein_matrix_file}{Character. Path to the protein-level intensity matrix
(tab-separated, rows = proteins, cols = samples).}

\item{samplesheet_file}{Character. Path to the sample sheet CSV describing
sample groups and pairwise comparisons.}

\item{out_dir}{Character. Directory where all outputs will be written.
Created if it does not already exist.}

\item{genome}{Character. Organism genome to use for annotation
(\code{"mouse"} or \code{"human"}). Default \code{"mouse"}.}

\item{imputation_method}{Character. Imputation strategy. One of
\code{"DEP-MinProb"}, \code{"DEP-BPCA"}, \code{"none"}, or a custom
method name. Default \code{"DEP-MinProb"}.}

\item{imputation_q}{Numeric. Quantile threshold passed to DEP imputation
methods. Default \code{0.01}.}

\item{imputation_seed}{Integer. Random seed for reproducible imputation.
Default \code{42}.}

\item{heatmap_top_n}{Integer. Number of top variable proteins to include
in the global heatmap. Default \code{1000}.}

\item{heatmap_norm}{Character. Normalisation applied before heatmap
(\code{"zscore"} or \code{"none"}). Default \code{"zscore"}.}

\item{gsea_ont}{Character. Gene Ontology domain for GSEA
(\code{"BP"}, \code{"MF"}, or \code{"CC"}). Default \code{"BP"}.}

\item{skip_gsea}{Logical. If \code{TRUE}, skip GSEA entirely.
Default \code{FALSE}.}

\item{skip_anova}{Logical. If \code{TRUE}, skip one-way ANOVA across all
groups. Default \code{FALSE}.}

\item{group_color1}{Character. Hex colour for the first group in plots.
Default \code{"#D55E00"}.}

\item{group_color2}{Character. Hex colour for the second group in plots.
Default \code{"#0072B2"}.}
}
\value{
Invisibly returns the path to the generated HTML report.
}
\description{
Reads protein-level intensity data, performs imputation, limma-based
pairwise differential analysis, PCA, optional ANOVA, GSEA, and writes
an HTML report plus all intermediate RDS/figure outputs.
}
\examples{
\dontrun{
run_regular_pipeline(
  run_id           = "exp_001",
  protein_matrix_file      = "data/protein_intensities.tsv",
  samplesheet_file = "data/samplesheet.csv",
  out_dir          = "results/exp_001"
)
}
}
