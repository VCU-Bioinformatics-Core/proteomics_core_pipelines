% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_ptm.R
\name{generate_volcano_ptm}
\alias{generate_volcano_ptm}
\title{Generate a PTM-Level Volcano Plot}
\usage{
generate_volcano_ptm(
  data,
  exp_name,
  ctrl_name,
  p_thresh = 0.05,
  lfc = 0.58,
  sig = "adj.P.Val",
  logFC_colname = "logFC",
  out_dir = ".",
  color1 = "#D55E00",
  color2 = "#0072B2"
)
}
\arguments{
\item{data}{Data frame of PTM differential results containing at minimum \code{logFC},
\code{P.Value}, \code{adj.P.Val}, and \code{peptide_id}.}

\item{exp_name}{Character. Experimental group name used in axis labels.}

\item{ctrl_name}{Character. Control group name used in axis labels.}

\item{p_thresh}{Numeric. Adjusted p-value threshold for significance. Default
\code{0.05}.}

\item{lfc}{Numeric. Minimum absolute log2 fold-change threshold. Default \code{0.58}.}

\item{sig}{Character. Column name to use for significance filtering. Default
\code{"adj.P.Val"}.}

\item{logFC_colname}{Character. Column name for log2 fold-change values. Default
\code{"logFC"}.}

\item{out_dir}{Character. Reserved output directory parameter (saving is the caller's
responsibility). Default \code{"."}.}

\item{color1}{Character. Colour for up-regulated points. Default \code{"#D55E00"}.}

\item{color2}{Character. Colour for down-regulated points. Default \code{"#0072B2"}.}
}
\value{
Named list with elements:
\describe{
\item{plot}{A \code{ggplot} volcano plot object.}
\item{highlighted_ids}{Character vector of the labelled peptide IDs (up to 20).}
}
}
\description{
Generate a PTM-Level Volcano Plot
}
\details{
Colours points by significance and fold-change direction, and labels the top
10 up- and down-regulated PTM peptides by adjusted p-value. Labels split the
\code{peptide_id} on \code{" -- "} to show a protein prefix plus a truncated
precursor suffix (e.g. \code{"P12345 -- AB...YZ"}).
}
