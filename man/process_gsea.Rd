% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_gsea.R
\name{process_gsea}
\alias{process_gsea}
\title{Run Gene Set Enrichment Analysis (GSEA) via gseGO}
\usage{
process_gsea(result, org_db, p = 0.05, lfc = 0.58, ont_option = "BP")
}
\arguments{
\item{result}{Data frame of limma results containing at minimum \code{logFC} and
\code{ensembl_gene_id} columns.}

\item{p}{Numeric. Adjusted p-value threshold (filtering of \code{result} is
expected upstream). Default \code{0.05}.}

\item{lfc}{Numeric. Log2 fold-change threshold (passed through). Default \code{0.58}.}

\item{ont_option}{Character. GO ontology to test: \code{"BP"} (default), \code{"MF"},
or \code{"CC"}.}
}
\value{
A \code{gseaResult} object with gene symbols resolved via
\code{setReadable}, or \code{NULL} if GSEA cannot be performed or yields no
significant terms.
}
\description{
Run Gene Set Enrichment Analysis (GSEA) via gseGO
}
\details{
Builds a ranked gene list from \code{result$logFC} (keyed by
\code{result$ensembl_gene_id}), drops genes with near-zero fold-change
(\code{|logFC| < 0.1}), and runs \code{clusterProfiler::gseGO}. Returns
\code{NULL} (with a warning) if there are fewer than two genes or no
enriched terms are found.
}
