% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_single_ptm_ida_comparison.R
\name{perform_multitest_ptm_ida_analysis}
\alias{perform_multitest_ptm_ida_analysis}
\title{Perform MSstatsPTM-Style PTM Differential Analysis with Protein-Level Correction}
\usage{
perform_multitest_ptm_ida_analysis(
  comparison,
  exp_samples,
  ctrl_samples,
  ptm_imp_matrix,
  protein_imp_matrix
)
}
\arguments{
\item{comparison}{Named list with elements \code{name}, \code{exp}, and \code{ctrl}
describing the contrast (same structure used elsewhere in the pipeline).}

\item{exp_samples}{Character vector. Sample names belonging to the experimental group.}

\item{ctrl_samples}{Character vector. Sample names belonging to the control group.}

\item{ptm_imp_matrix}{Data frame in long format with columns \code{peptide_id},
\code{PG.UniProtIds}, \code{sample}, and \code{intensity}.}

\item{protein_imp_matrix}{Data frame in long format with columns
\code{PG.ProteinAccessions}, \code{sample}, and \code{intensity}.}
}
\value{
Data frame with one row per (protein, PTM peptide) pair and columns:
\code{uniprot_id}, \code{peptide_id}, \code{logFC_ptm}, \code{logFC_prot},
\code{logFC} (corrected), \code{AveExpr}, \code{SE_ptm}, \code{SE_prot},
\code{DF_ptm}, \code{DF_prot}, \code{t}, \code{P.Value}, \code{adj.P.Val}.
}
\description{
Perform MSstatsPTM-Style PTM Differential Analysis with Protein-Level Correction
}
\details{
For each UniProt protein, fits separate linear models at the protein level
and at each PTM peptide level, then computes a protein-corrected PTM log fold-change
(\code{logFC_ptm - logFC_prot}) with Welch-Satterthwaite degrees of freedom.
Adjusted p-values are computed via Benjamini-Hochberg across all PTM-protein pairs.

Note: currently iterates over the first 50 unique protein IDs (debug limit on line 43).
}
