% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helpers.R
\name{generate_heatmap}
\alias{generate_heatmap}
\title{Generate a Per-Comparison Differential Abundance Heatmap}
\usage{
generate_heatmap(
  diff_df,
  intensity_df,
  p = 0.05,
  lfc = 0.58,
  exp_name,
  ctrl_name,
  fig_dir,
  design,
  row_id_col = "uniprotswissprot",
  heatmap_norm = "zscore",
  color1 = "#D55E00",
  color2 = "#0072B2"
)
}
\arguments{
\item{diff_df}{Data frame of limma results (output of \code{\link{perform_limma_analysis}})
containing at minimum \code{adj.P.Val}, \code{logFC}, and the column named by
\code{row_id_col}.}

\item{intensity_df}{Numeric data frame or matrix (features x samples) of log2 intensities.
Row names must match values in \code{diff_df[[row_id_col]]}.}

\item{p}{Numeric. Adjusted p-value threshold for significance. Default \code{0.05}.}

\item{lfc}{Numeric. Minimum absolute log2 fold-change threshold. Default \code{0.58}
(~1.5-fold).}

\item{exp_name}{Character. Column name in \code{design} identifying experimental samples.}

\item{ctrl_name}{Character. Column name in \code{design} identifying control samples.}

\item{fig_dir}{Character. Directory for figure output (saving is the caller's
responsibility).}

\item{design}{Model matrix (samples x groups) used to identify which samples belong to
each group.}

\item{row_id_col}{Character. Column in \code{diff_df} whose values index rows of
\code{intensity_df}. Default \code{"uniprotswissprot"}.}

\item{heatmap_norm}{Character. \code{"zscore"} (default) or any other value for raw
intensity.}

\item{color1}{Character. Reserved colour parameter for consistency. Default
\code{"#D55E00"}.}

\item{color2}{Character. Reserved colour parameter for consistency. Default
\code{"#0072B2"}.}
}
\value{
A \code{ComplexHeatmap::Heatmap} object ready to be drawn with \code{draw()}.
}
\description{
Generate a Per-Comparison Differential Abundance Heatmap
}
\details{
Filters \code{diff_df} to significant features (\code{adj.P.Val < p} and
\code{|logFC| >= lfc}), subsets \code{intensity_df} to the relevant samples, adds
a small jitter to break ties, and returns a \code{ComplexHeatmap::Heatmap} object
(not yet drawn or saved).
}
